-- ============================================================
-- RBAC 权限体系二期 · 增量迁移脚本（byw_user 库）
-- 用途：在【一期 RBAC 结构】基础上新增平台端「菜单管理」菜单（sys:menu）
-- 特性：可重复执行（幂等）——种子用 INSERT IGNORE
-- ============================================================
USE byw_user;

-- ------------------------------------------------------------
-- 1. 新增平台端菜单：系统管理(190) → 菜单管理(193)
--    说明：菜单管理默认仅超管可用（super_admin 走 * 通配无需绑定）；
--          其他平台角色如需使用，由超管在「角色管理-菜单授权」中自行分配
-- ------------------------------------------------------------
INSERT IGNORE INTO t_sys_menu (id, parent_id, menu_name, menu_type, scope, path, perm_code, icon, sort_order) VALUES
(193, 190, '菜单管理', 2, 'platform', '/system/menu', 'sys:menu', 'Menu', 93);

-- ------------------------------------------------------------
-- 校验
--   SELECT id, parent_id, menu_name, perm_code FROM t_sys_menu WHERE id = 193;
-- ------------------------------------------------------------
